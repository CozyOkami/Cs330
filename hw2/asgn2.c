#include <stdio.h>
#include "asgn2.h"

int main(){
    int n;
    int func;
    int length;
    int width;
    int height;
    int avg_exams;
    int avg_hw;
    int attendance;
    cubeOfodd(n);
    introToCS330(n);
    printHello(n);
    paintGallons(length,width,height);
    grader(avg_exams,avg_hw,attendance);
    return 0;
}